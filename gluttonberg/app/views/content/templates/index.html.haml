- @page_title = "Templates"

- if @templates.empty? && @layouts.empty?
  %p.empty No Templates
- else
  %table{:cellspacing => 0, :cellpadding => 0}
    %thead
      %tr
        %th Name
        %th Format
        %th.controls Controls
    %tbody
      - unless @templates.empty?
        %tr
          %th{:colspan => 3} Templates
        - for template in @templates
          %tr
            %td= template.name
            %td= template.format
            %td.controls
              %ul
                %li= link_to("Edit", slice_url(:edit_template, template))
                %li= link_to("Edit Sections", slice_url(:template_sections, :template_id => template))
                %li= link_to("Delete", slice_url(:delete_template, template))
      - unless @layouts.empty?
        %tr
          %th{:colspan => 3} Layouts
        - for template in @layouts
          %tr
            %td= template.name
            %td= template.format
            %td.controls
              %ul
                %li= link_to("Edit", slice_url(:edit_template, template))
                %li= link_to("Delete", slice_url(:delete_template, template))

%p.newLink= link_to("New", slice_url(:new_template))